import { FilterTableTimeslotPipe } from './filter-table-timeslot.pipe';

describe('FilterTableTimeslotPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTableTimeslotPipe();
    expect(pipe).toBeTruthy();
  });
});
